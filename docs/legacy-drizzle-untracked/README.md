# Archived untracked Drizzle SQL

These SQL files were present in the uploaded V96 package but were not registered in `drizzle/meta/_journal.json` and overlap older authentication/registration experiments. They are archived as `.txt` so migration tooling cannot apply them accidentally. The active migration chain is `0000`–`0004`, with `0004_standalone_auth.sql` matching the staging deployment migration.
